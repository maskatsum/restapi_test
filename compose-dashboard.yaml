volumes:
  _volumes:
  _sessions:

networks:
  internal:
    driver: bridge
    
services:
   dashboard:
     build:
       context: ./demo/   
     environment:
       REALM_NAME: kana-api
       KEY_CLOAK_AUTH_URL: http://ec2-35-76-34-50.ap-northeast-1.compute.amazonaws.com/auth
     ports:
       - 80:8080
